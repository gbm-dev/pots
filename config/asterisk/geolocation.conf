; Geolocation configuration for Asterisk 22
; A minimal configuration is required for res_geolocation to load

[general]
; No specific general settings required for basic operation

; Empty [location] or [profile] sections can sometimes cause issues
; so we define at least a basic global profile
[default_profile]
type=profile
